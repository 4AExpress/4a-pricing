# CMS Slab Rate Import Format

> Specification of the CSV format accepted by CMS at:
> Invoice → Client Rate Import

## Overview

CMS imports per-customer pricelists as CSV with one row per **weight bracket** per (origin, destination, service, docs type).

Slab Rate is **cumulative**, not flat.

## Column headers (exact, in order)
